import { AccessTokenService } from './AccessTokenService';
type ApplicationAccessTokenServiceOptions = {
    clientId: string;
    authAppsServiceUrl?: string;
    origin?: string;
};
export declare class ApplicationAccessTokenService extends AccessTokenService {
    private authAppsServiceUrl;
    private clientId;
    private accessToken?;
    private origin?;
    constructor({ clientId, authAppsServiceUrl, origin, }: ApplicationAccessTokenServiceOptions);
    private refreshAccessToken;
    getAccessToken: () => Promise<string>;
}
export {};
