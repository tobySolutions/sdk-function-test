import { AccessTokenService } from './AccessTokenService';
type PersonalAccessTokenServiceOptions = {
    graphqlServiceApiUrl?: string;
    personalAccessToken: string;
    projectId?: string | undefined;
};
export declare class PersonalAccessTokenService extends AccessTokenService {
    private client;
    private personalAccessToken;
    private projectId?;
    private accessTokenTimeout?;
    private accessToken?;
    constructor({ personalAccessToken, projectId, graphqlServiceApiUrl, }: PersonalAccessTokenServiceOptions);
    private setAccessToken;
    private loginWithPersonalAccessToken;
    getAccessToken: () => Promise<string>;
    close: () => void;
}
export {};
